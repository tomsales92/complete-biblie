# Server (legado)

Este backend Express com `db.json` foi substituído pelo **Firebase** (Auth + Firestore).

O frontend agora se comunica diretamente com o Firebase. Esta pasta pode ser ignorada em produção.

Para rodar localmente (apenas para referência):

```bash
npm install
npm start
```
